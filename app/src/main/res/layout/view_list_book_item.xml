<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent">

    <RelativeLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/rl_book"
    >

        <ImageView
                android:layout_width="90dp"
                android:layout_height="90dp"
                android:id="@+id/iv_delete_icon"
                android:scaleType="centerInside"
                android:src="@mipmap/icon_delete"
                android:layout_alignParentRight="true"
        />

        <ImageView
                android:layout_width="90dp"
                android:layout_height="90dp"
                android:id="@+id/iv_book_icon"
                android:scaleType="centerInside"
                android:layout_toLeftOf="@id/iv_delete_icon"
        />



        <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/tv_book_title"
                android:textSize="20sp"
                android:textColor="#0A0A0A"
                android:layout_marginLeft="10dp"
                android:maxLines="1"
                android:ellipsize="end"
                android:layout_alignBottom="@id/iv_book_icon"
                android:layout_alignTop="@id/iv_book_icon"
                android:layout_toLeftOf="@id/iv_book_icon"
                android:gravity="center|left"
        />

    </RelativeLayout>
</LinearLayout>